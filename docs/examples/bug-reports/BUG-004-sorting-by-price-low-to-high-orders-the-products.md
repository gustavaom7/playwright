# [Flaky] sorting by price low to high orders the products: Timeout Nms exceeded while waiting on the predicate

| Field | Value |
| --- | --- |
| Type | Bug |
| Severity | Low |
| Priority | Low |
| Labels | automated-triage, flaky, playwright |
| Environment | https://www.saucedemo.com (projects: chromium-auth) |
| Test | `authenticated/catalog.spec.ts:30` |

## Description
The automated test "sorting by price low to high orders the products" failed and only passed after a retry (flaky) in 1 project(s).

## Steps to reproduce
1. `npm ci && npx playwright install --with-deps`
2. `npx playwright test authenticated/catalog.spec.ts -g "sorting by price low to high orders the products" --project=chromium-auth`
3. Compare with the expected and actual results below.

## Expected result
The test assertion passes.

## Actual result
(see error)

```text
Timeout 5000ms exceeded while waiting on the predicate
```

## Evidence
- No screenshot/trace attached (run with `--trace on` to capture one).

## Notes
- Draft generated by `scripts/triage-failures.ts`; review before filing.
