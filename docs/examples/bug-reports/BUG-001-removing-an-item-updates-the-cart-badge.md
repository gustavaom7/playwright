# [Functional] removing an item updates the cart badge: expect(locator).toHaveText(expected)

| Field | Value |
| --- | --- |
| Type | Bug |
| Severity | Critical |
| Priority | Highest |
| Labels | automated-triage, functional, playwright |
| Environment | https://www.saucedemo.com (projects: chromium-auth, firefox-auth, webkit-auth) |
| Test | `authenticated/cart-and-checkout.spec.ts:42` |

## Description
The automated test "removing an item updates the cart badge" fails in 3 project(s).

## Steps to reproduce
1. `npm ci && npx playwright install --with-deps`
2. `npx playwright test authenticated/cart-and-checkout.spec.ts -g "removing an item updates the cart badge" --project=chromium-auth`
3. Compare with the expected and actual results below.

## Expected result
"1"

## Actual result
"2"

```text
expect(locator).toHaveText(expected)

Expected string: "1"
Received string: "2"
Timeout: 5000ms
```

## Evidence
- screenshot: `test-results/cart-chromium/screenshot.png`

## Notes
- Draft generated by `scripts/triage-failures.ts`; review before filing.
