# [Performance] the inventory page loads within its time budget: expect(received).toBeLessThan(expected)

| Field | Value |
| --- | --- |
| Type | Bug |
| Severity | Medium |
| Priority | Medium |
| Labels | automated-triage, performance, playwright |
| Environment | https://www.saucedemo.com (projects: chromium-auth, mobile-chrome) |
| Test | `authenticated/performance.spec.ts:6` |

## Description
The automated test "the inventory page loads within its time budget" fails in 2 project(s).

## Steps to reproduce
1. `npm ci && npx playwright install --with-deps`
2. `npx playwright test authenticated/performance.spec.ts -g "the inventory page loads within its time budget" --project=chromium-auth`
3. Compare with the expected and actual results below.

## Expected result
< 3000

## Actual result
3412

```text
expect(received).toBeLessThan(expected)

Expected: < 3000
Received: 3412
```

## Evidence
- No screenshot/trace attached (run with `--trace on` to capture one).

## Notes
- Draft generated by `scripts/triage-failures.ts`; review before filing.
